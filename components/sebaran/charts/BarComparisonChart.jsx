import {
  ResponsiveContainer,
  PieChart,
  Pie,
  BarChart,
  Bar,
  LineChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
} from "recharts";

export default function BarComparisonChart() {

  return (
    <ResponsiveContainer width="100%" height={300}> 
    
    </ResponsiveContainer>);
}