"use client";
import { useEffect, useState } from "react";

export function useDetailPTK(id, open) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(false);

  useEffect(() => {});
  return {};
}
